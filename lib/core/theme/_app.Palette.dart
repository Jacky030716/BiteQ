import 'package:flutter/material.dart';

class Palette {
  static final Color primary = Colors.blue[900] ?? Colors.blue;
  static final Color placeholder = Colors.grey[500] ?? Colors.grey;
  static final Color error = Colors.red[500] ?? Colors.red;
  static final Color background = Colors.white;
  static final Color transparent = Colors.transparent;

  static final Color whiteText = Colors.white;
  static final Color blackText = Colors.black;
}
